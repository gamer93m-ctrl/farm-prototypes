# Генератор — логика работы

Кликабельный прототип **только экрана генератора**, для проверки гипотез. Вся логика на клиенте.

Аналогия из жизни: **грядки**. Пользователь сажает ресурс в грядку, тот растёт заданное время, потом его собирают, грядка освобождается.

Макет: [Figma, «Генератор MVP»](https://www.figma.com/design/06VT2jyVr85l9tXnFs2QpM/Untitled?node-id=2-13190) · мобилка 360×780.
Прототип: `index.html` — открывается двойным кликом, без сборки и сервера.

---

## 1. Сущности

| Сущность | Что это |
|---|---|
| **Ячейка** (грядка) | Слот, в который сажают ресурс. Единица взаимодействия. |
| **Блок** | Сетка **5×5 = 25 ячеек**. Страница листалки. |
| **Тип ресурса** | Что сажают. У каждого типа своё время генерации. |
| **Лоток** | Список типов внизу экрана. Из него тащат ресурс на ячейку. Не склад: типы не расходуются. |
| **Инструмент сбора** | Круглая кнопка «ручка» 👋 справа от пейджера. Им собирают созревшее. |
| **Выбранная ячейка** | Ячейка, отмеченная тапом (синяя заливка). Меняет поведение перетаскивания — см. правило 2. |

---

## 2. Состояния ячейки

Все ячейки в прототипе открыты — состояния «недоступна» нет, открытие ячеек вне скоупа.

```
                  ┌──────────┐
     ┌───────────▶│  EMPTY   │  пустая, готова к посеву
     │            └────┬─────┘
     │                 │ посев: перетаскивание ресурса из лотка
     │                 ▼
     │            ┌──────────┐
     │            │ GROWING  │  растёт, тикает таймер
     │            └────┬─────┘
     │                 │ таймер истёк
     │                 ▼
     │            ┌──────────┐
     └────────────┤  READY   │  выросло, можно собирать
        сбор      └──────────┘
```

**Из GROWING выхода назад нет.** Засеял — жди. Отмена и пересадка не предусмотрены.

Визуально: `EMPTY` — пустая плашка; `GROWING` — ресурс приглушён (40%) и по кругу растёт дуга прогресса; `READY` — ресурс просто вырос, показан в полную силу. Никакой дополнительной подсветки готовности нет.

---

## 3. Посев — кор-механика

Перетаскивание **массовое**: зажал ресурс в лотке и ведёшь пальцем по сетке — засеиваются все ячейки под пальцем, а не одна.

Двигается только изображение ресурса; рамка в лотке остаётся на месте.

**Правила (примечание из макета, узел `2:17325`):**

1. При перетаскивании ресурса на пустую ячейку — запускается генерация.
2. Если предварительно была выбрана конкретная ячейка, запуск генерации начинается **только с этой ячейки**. Пока она не отжата или в неё не помещена генерация — другие пустые ячейки не активируются.
3. При попадании ресурса на ячейку в статусе «в процессе» — ничего не происходит.

---

## 4. Генерация

Время задаётся **типом ресурса**. Ячейки тикают независимо. По истечении таймера ячейка сама переходит в `READY`.

```js
const RESOURCE_TYPES = [
  { id:'strawberry', name:'Клубника', growMs:10*1000, image:'img.png' },
  { id:'banana',     name:'Банан',    growMs:20*1000, image:'imgb.png' },
  { id:'coffee',     name:'Кофе',     growMs:40*1000, image:'imgcoffe.png' },
];
```

---

## 5. Сбор

Собирают **инструментом сбора** — той же механикой перетаскивания, тоже массовой.

**Правила (примечание из макета, узел `2:18720`):**

1. При перетаскивании инструмента на ячейку с готовой генерацией — производится сбор.
2. Если предварительно была выбрана конкретная ячейка, процесс сбора начинается **с этой ячейки**. Пока она не отжата или не собрана — другие готовые генерации не собираются.
3. При попадании инструмента на ячейку в статусе «в процессе» — ничего не происходит.

Три контекста сбора из макета ложатся на эти правила так:

| Контекст | Как получается |
|---|---|
| Сбор без выбора | Провёл инструментом по готовым — собрались все подряд. |
| Сбор с выбором ячейки | Ячейка выбрана заранее → сбор идёт с неё, остальные заперты до её обработки. |
| Двойной тап на ячейку | Быстрый сбор одной готовой ячейки, без инструмента. |

Результат везде один: `READY → EMPTY`. Куда девается собранное — **вне этого флоу**, склада в прототипе нет.

---

## 6. Обратная связь при перетаскивании

Ячейка под пальцем слегка поджимается (scale 0.88) с мягким баунсом — и при посеве, и при сборе. Это единственный отклик; подсветок и свечений нет.

---

## 7. Два режима прототипа

Переключалка — в шапке, слева от счётчиков: `1` / `4` / `⟲` (сброс). Оформлена пунктиром: это служебный контрол, не часть продуктового интерфейса.

| Режим | Что показываем |
|---|---|
| **1 блок** | Одна сетка 5×5, 25 ячеек. Листать некуда, пейджер скрыт. |
| **Оверфлоу** | 4 блока, 100 ячеек. Появляется листалка и пейджер. |

При загрузке — скелетон.

---

## 7.1 Прокрутка блоков

**Якорная позиция.** Блок с генерациями всегда встаёт по центру экрана — при любом способе прокрутки. По краям выглядывают соседние блоки (зоны 33px).

Три жеста:

| Жест | Поведение |
|---|---|
| **Свайп** влево/вправо по сетке | Обычная прокрутка со снапом на соседний блок. |
| **Тап по краю экрана** | Левая или правая зона 33px — переход на один блок. Крайние зоны гаснут на первом и последнем блоке. |
| **Долгий тап на pager** | Через 320 мс pager подрастает (×1.25). Не отрывая палец, ведёшь вправо-влево — точная плавная навигация по всей ширине экрана. На отпускании возврат к якорной позиции. |
| **Удержание у края во время драга** | Подвёл ресурс или инструмент к краевой зоне и держишь — через 400 мс листается блок, дальше каждые 700 мс. Палец не отрывается, посев продолжается в новом блоке. Зона подсвечивается синим, пока взведена. |

> Последний жест **в макете не описан** — добавлен, чтобы можно было засеять больше 25 ячеек за один заход. Тайминги 400/700 мс подобраны на глаз и правятся константами `EDGE_DELAY` / `EDGE_REPEAT`.

Во время быстрой прокрутки снап временно отключается, иначе он мешает вести палец плавно. Тап-зоны краёв отключаются на время перетаскивания ресурса или инструмента, чтобы не перехватывать посев и сбор.

---

## 8. Геометрия (из макета, узлы `2:14108` и `2:16080`)

| Элемент | Значение |
|---|---|
| Экран | 360×780 |
| Ячейка | 56×56, радиус 6, фон `rgba(255,255,255,.1)` |
| Гэп ячеек | 2px · блок 288×288 |
| Сетка | left 36, top 216 · гэп блоков 20 |
| Пейджер | @133,536 · 95×20, точка активная `#428bf9` |
| Инструмент сбора | @272,528 · 52×52 |
| Лоток | @0,594 · items 80×80, x от 40, шаг 92 |
| Заголовок | SF Pro Display Bold 30, tracking .36 |
| Подзаголовок | SF Pro Text 17, tracking −.41 |
| Бейджи | h28, radius 100, SF Pro Text Semibold 13 |
| Выбранная ячейка | заливка `rgba(66,139,249,.2)` |

---

## 9. Вне скоупа

- Склад, инвентарь, количества собранного.
- Валюты и счётчики в шапке — статика.
- Открытие новых ячеек и кнопка «+».
- Замки в лотке и разблокировка типов — все типы доступны сразу.
- Уровни и прогрессия.
- Ускорение генераций.
- Карта фермы, постройки, прочие экраны.
- Любая серверная логика.

---

## Разделы макета

Отдавать по одной секции, не файл целиком (~5700 узлов).

| Раздел | Ссылка |
|---|---|
| Новый вид MVP | [2-13192](https://www.figma.com/design/06VT2jyVr85l9tXnFs2QpM/Untitled?node-id=2-13192) |
| Новые ячейки | [2-13663](https://www.figma.com/design/06VT2jyVr85l9tXnFs2QpM/Untitled?node-id=2-13663) |
| Тултипы и тапы на ячейки | [2-14105](https://www.figma.com/design/06VT2jyVr85l9tXnFs2QpM/Untitled?node-id=2-14105) |
| Скролл | [2-15062](https://www.figma.com/design/06VT2jyVr85l9tXnFs2QpM/Untitled?node-id=2-15062) |
| Прототип (14 экранов) | [2-16290](https://www.figma.com/design/06VT2jyVr85l9tXnFs2QpM/Untitled?node-id=2-16290) |
| На карте | [2-17093](https://www.figma.com/design/06VT2jyVr85l9tXnFs2QpM/Untitled?node-id=2-17093) |
| Генерация: drag-and-drop | [2-17109](https://www.figma.com/design/06VT2jyVr85l9tXnFs2QpM/Untitled?node-id=2-17109) |
| Генерация: user-flow | [2-17777](https://www.figma.com/design/06VT2jyVr85l9tXnFs2QpM/Untitled?node-id=2-17777) |
| Сбор ресурсов | [2-18263](https://www.figma.com/design/06VT2jyVr85l9tXnFs2QpM/Untitled?node-id=2-18263) |

---

## 10. Вторая версия механики (только `/generator/`)

Модуль общий, но развивать надо было один прототип: онбординги уже отданы
на тест и меняться не должны. Поэтому вторая версия живёт под флагом.

```js
createGenerator(host, { v: 2 });   // ставит только generator/index.html
```

Внутри — `const V2 = opts.v === 2`, в разметке `#phone.v2`. Всё новое пишется
как `if(V2){ новое } else { как было }`, стили — под селектором `.v2`.
Онбординги флаг не передают и работают ровно как раньше.

### B · удержание: видно, что режим взвёлся

Раньше удержание было немым: держишь палец и не знаешь, сработало или нет.
Стало три сигнала подряд:

Ручка `👋` из версии A стоит на своём месте всегда: экран сразу говорит,
чем тут собирают, даже если ты ещё ничего не трогал.

| Момент | Что видно |
|---|---|
| Держишь | Из центра ячейки разрастается синее пятно — зарядка на 320 мс. |
| Взвелось | Ручка улетает с кнопки на палец и висит на нём, пока ведёшь. |
| Отпустил | Ручка возвращается на кнопку. |

Ручка тут не инструмент, а индикатор: тащить её, как в версии A, нельзя —
она сама показывает, что сейчас идёт массовый сбор. Удержание подняли
с 250 до 320 мс: за 250 зарядка не успевает прочитаться.

Режимы «водишь пальцем» и «с готовой» не изменились: там ждать нечего,
и ручка не нужна.

### C · выделение → режим дёрганья

Галочек и панели «Выбрано N» больше нет. Вместо выбора — режим, как на
домашнем экране айфона:

1. Долгое удержание (450 мс, с той же зарядкой) включает режим сбора.
2. Все готовые ячейки начинают дёргаться, каждая со своей фазой.
   Растущие и пустые уходят на второй план — приглушаются.
3. Дальше тапаешь по дёргающимся или ведёшь по ним пальцем — они
   выщёлкиваются по одной. Палец можно не отрывать: ячейка под ним
   собирается сразу при входе в режим.
4. Внизу панель: «Собрать всё · N» и «Готово».
5. Режим гаснет сам, когда готовых не осталось; вручную — «Готово» или
   тап мимо сетки.

Место под панель держится всегда: иначе её появление сдвигало сетку прямо
под пальцем и ячейка уезжала из-под касания.
